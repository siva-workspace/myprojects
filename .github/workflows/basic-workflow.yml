name: basic-workflow
on: workflow_dispatch
jobs:
 first-job:
  runs-on: ubuntu-latest
  steps:
   - name: print cmds
     run: echo "first run"
   - name: print multi cmds
     run: |
         echo "First output"
         echo "Second output"
